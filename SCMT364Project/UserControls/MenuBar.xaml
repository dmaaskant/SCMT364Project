<UserControl x:Class="SCMT364Project.UserControls.MenuBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SCMT364Project.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="45" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
        </Grid.RowDefinitions>
        <Menu>
            <MenuItem Header="Workstations"  Height="70" FontSize= "20" VerticalAlignment="Center"/>
            <!--<MenuItem Header="Factory Location" Height="70" FontSize="20" VerticalAlignment="Center"/>-->
            <MenuItem Header="Exit Application" Height="70" FontSize="20" VerticalAlignment="Center" Click="MenuItem_Click"/>


        </Menu>
        <!--<Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <Button Width="50" Margin="5"/>
            <TextBox Width="150" Grid.Column="1" Margin="5"/>
        </Grid>-->
    </Grid>
</UserControl>
